name: Playwright2 Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Run POST tests
      run: npx playwright test tests/POST_add_pet.spec.js
    - name: Run GET tests
      run: npx playwright test tests/GET_pet_byID.spec.js
    - name: Run PUT tests
      run: npx playwright test tests/PUT_Update_byID.spec.js
    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
